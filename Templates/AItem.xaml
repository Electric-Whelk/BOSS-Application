<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="OBTemplate">
        <Border Margin="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding Description}"/>
                <CheckBox Grid.Column="1" IsChecked="{Binding Noticed}"
                          IsEnabled="{Binding CanTick}"
                          Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window},
             Path=DataContext.SortItemsPerTick}"/>
                <!--<TextBlock Grid.Column="1" Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window},
             Path=DataContext.Strobe}"/>-->

            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="CBTemplate">
        <Border Margin="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding Description}"/>
                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="-" Command="{Binding TickDown}"/>
                    <TextBox Grid.Column="1" Text="{Binding Count}"/>
                    <Button Grid.Column="2" Content="+" Command="{Binding TickUp}"/>

                </Grid>
            </Grid>
        </Border>
    </DataTemplate>


</ResourceDictionary>